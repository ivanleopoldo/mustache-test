{
  "name": "test-template",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun run utils.ts",
    "serve": "bun run build && bun run server.ts"
  },
  "devDependencies": {
    "@types/browserify": "^12.0.40",
    "@types/bun": "latest",
    "@types/mustache": "^4.2.5",
    "@types/serve-handler": "^6.1.4",
    "tsify": "^5.0.4"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "browserify": "^17.0.0",
    "mustache": "^4.2.0",
    "serve-handler": "^6.1.5"
  }
}
